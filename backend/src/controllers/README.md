# `src/controllers`

- **`contentController.jsx`** — AI `/api/content/generate` (GET help + POST) handlers.
- **Assessment, flashcards, MCQ bank** — logic is still inside `src/routes/*.jsx` (can be moved here later for a stricter MVC layout).

පරණ `StudentsPerformance*` controller files git history එකේ delete වෙලා තිබුණා; දැන් performance + exams `assessmentRoutes.jsx` තුළ handle වෙනවා.
